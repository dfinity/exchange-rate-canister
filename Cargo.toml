[workspace]
members = [
    "src/xrc",
    "src/ic-xrc-types",
    # crates used for development
    "src/xrc-tests",
    "src/monitor-canister",
    "src/monitor-canister-exporter",
]

[profile.release]
lto = true
opt-level = 'z'
